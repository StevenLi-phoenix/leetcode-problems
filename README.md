# leetcode-problems

My LeetCode solutions, managed with [leetcode-cli](https://github.com/StevenLi-phoenix/leetcode-cli).

Organized as `{difficulty}/{category}/{id}.{slug}.{ext}`. Each file carries a
metadata header so `leetcode test <id>` / `leetcode submit <id>` work from here.
